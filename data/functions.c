// comentairo pica

/* super* linhas comentario
comentario */
int function_placeholder(int var1, float var2) {
    var1 += (int)var2;
    float var3 = var2 / 0;
    float penis = 1e-10;
    for (int i = 0; i < var1; i++) {
        var3 += i * 1.5;
    }
    while (var3 > 0)
    {
        var3 -= 0.5;
        printf("Current var3: %.2f\n", var3);
        char chartest = 'A';
        printf("Character test: %c\n", chartest);
    }
    
}